# DEL-MSG-01: Send a Bounded Mission Capsule

Every subagent-dispatch and direct teammate-message body must stay at or below 4,096 characters. Include exact paths, constraints, expected result, and the standards the recipient must read recursively.

For longer assignments, write a durable, secret-free task artifact and send its absolute path plus at most two summary lines.
