# AUT-BOUN-01: Use Semantic Content Boundaries

Enclose each important or long block in a semantically named XML tag so it cannot bleed into surrounding prose. The tag identifies the block's role rather than copying the section heading.

Use `<report>` for a machine-readable report or output contract and `<IMPORTANT>` for a hard guardrail or critical instruction.
