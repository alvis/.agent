# AUT-BOUN-02: Preserve Structure Inside Boundaries

Boundary tags supplement Markdown structure; they never replace headings. Keep a language hint on every fenced block inside a boundary because the tag establishes scope while the fence identifies syntax. Every opening tag must have a matching close.
